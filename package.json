{
  "name": "reduce-to",
  "version": "0.0.3",
  "description": "convert and modify Arrays, Objects, Maps, Sets, Strings and iterators",
  "keywords": [
    "transducer", "reducer", "transduce", "reduce", "convert", "key", "value"
  ],
  "author": "Hugo Villeneuve",
  "main": "./xfo.js",
  "dependencies": {
  },
  "devDependencies": {
    "cotest": "^1.6.1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/hville/reduce-to.git"
  },
  "scripts": {
    "test": "cotest test",
    "min": "uglifyjs ./index.js -c unsafe,drop_console -m --screw-ie8 --preamble //hugov@runbox.com --wrap reduceTo -o ./index.min.js"
  },
  "private": false,
  "license": "MIT",
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  }
}
